# MARKETING Task Guidance

Outward-facing work that makes claims to strangers: landing pages, launch posts, ads,
sales and pricing copy, positioning pages, store listings, go-to-market assets.
Composes onto `PROSE_FLOOR.md` + `WRITING.md` (marketing copy is human-voiced prose;
those files carry the register and accuracy gates, so this file does not repeat them).

## Quality Gates

| Aspect | Threshold |
|--------|-----------|
| Claim fidelity to the North Star | Every outward-facing claim the deliverable makes is consistent with the `Promise` in the project's North Star (its standing strategy doc, `NORTH_STAR.md`) and crosses none of its `Never` entries |
| Arrival–offer match | The deliverable names the arrival it is written for — what the reader was doing and wanting when they got here — and its opening addresses that arrival; where the North Star's `How they arrive` names arrivals, the one addressed is among them |

**Claim fidelity** encodes as a gate whose body has the evaluator read the project's
`NORTH_STAR.md` (root, or wherever the project context file says it lives) and the
finished deliverable, and check every claim a stranger would take as a promise — what
the product does, for whom, at what price — against the `Promise` and `Never` fields.
A price stated anywhere in the deliverable must match the North Star's `Money` field
exactly: a different number is a different offer. **When the project has no North Star,
the gate does not silently pass:** the manifest must itself state the promise the
deliverable is allowed to make and the claims it must not, and the evaluator checks
against that manifest text instead — a marketing deliverable with no stated promise
anywhere has nothing to be checked against and FAILs until one is written down.

**Arrival–offer match** exists because a conversion belongs to the pair — who arrived,
and what they were offered — so copy matched to one arrival and shown to another
measures nothing. The evaluator judges from the deliverable and the North Star (or the
manifest's stated arrival) alone.

Both are Judgment Gates.

## Defaults

*Domain best practices for this task type.*

- **Write to the arrival before drafting** — name where the reader was and what they
  wanted; the opening line answers that, not the product's self-description
- **One promise per surface** — lead with the promise sentence; stacked claims dilute
  the one that converts
- **Claims a stranger can check** — prefer the demonstrable ("verifies every criterion
  independently") over the aspirational ("writes perfect code"); the reader has been
  burned before
