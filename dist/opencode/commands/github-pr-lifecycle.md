---
description: "Read-only inspection of a single GitHub PR lifecycle — CI, review threads, description sync, and mergeability — returning PASS or FAIL with per-gate findings. Never invokes the merge button."
---

Invoke the manifest-dev:github-pr-lifecycle skill with: "$ARGUMENTS"
