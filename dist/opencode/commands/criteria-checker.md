---
description: "Verify a single criterion (PASS/FAIL/BLOCKED) against a verify prompt using any available tool. Read-only — never modifies files."
---

Invoke the manifest-dev:criteria-checker skill with: "$ARGUMENTS"
