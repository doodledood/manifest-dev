---
description: "Tend a PR through review to merge-readiness. Classifies comments, fixes issues via manifest amendment + scoped /do, tends CI, and asks before merging."
---

Invoke the manifest-dev:tend-pr skill with: "$ARGUMENTS"
