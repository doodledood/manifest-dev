{
  "source_commit": "11fa3a0f266d707682adbb7e83f1d769c11d15a9",
  "source_path": "claude-plugins/manifest-dev",
  "source_paths": [
    "claude-plugins/manifest-dev",
    "claude-plugins/manifest-dev-tools"
  ],
  "target": "pi",
  "synced_at": "2026-06-10T15:24:29Z",
  "notes": "Full re-sync of the Pi target. dist/pi/skills/ regenerated from source as the compatible shared-skill set of exactly 13 skills: from manifest-dev (core) check-pr, define, figure-out, figure-out-team, poll-slack, review-code; from manifest-dev-tools adr, handoff, prompt-engineering, review-pr, review-prompt, teach-me, walk-pr. Excluded per pi-cli.md: do/done/escalate (runtime-owned outcomes) and auto/babysit-pr (extension-command wrappers). Body adaptation applied only to define's completion handoff template: dropped the Claude 'Session: ~/.claude/projects/<dir>/${CLAUDE_SESSION_ID}.jsonl' line (Pi has no per-session file the agent locates at runtime) and the '/goal /do <manifest-path>' unattended-execution line (no Pi equivalent); the '/do <manifest-path>' handoff is kept and resolves to the native Pi extension command. review-code's 13 dimension references preserved. No agents anywhere (manifest-dev ships none): component-namespaces.json records agents == {} with an agents_note, and verification is always a general-purpose subagent activating a skill via verify.prompt (no verify.agent field). component-namespaces.json skills map exactly equals the 13 skills with core/tools ownership; keeps runtime_owned_skills (do/done/escalate), extension_wrappers (auto/babysit-pr), commands (do/auto/babysit-pr), tools {}, runtime_internal, and runtime_dependencies. README reflects the two-package runtime (@doodledood/manifest-dev-pi for /do and /auto plus the shared Harness-level Do runtime; @doodledood/manifest-dev-pi-tools for /babysit-pr), shared skills, no verify.agent field, no --manifest-verifier-agent flag, and install via 'pi install git:github.com/doodledood/manifest-dev@main' plus the 'pi install npm:@gotgenes/pi-subagents' verifier-fanout prerequisite. Repo-root package.json and pi/extensions/ left untouched (source-owned)."
}
