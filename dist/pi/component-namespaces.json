{
  "skills": {
    "adr": "manifest-dev-tools",
    "code-review": "manifest-dev",
    "define": "manifest-dev",
    "figure-out": "manifest-dev",
    "figure-out-team": "manifest-dev",
    "handoff": "manifest-dev-tools",
    "prompt-engineering": "manifest-dev-tools",
    "review-pr": "manifest-dev-tools",
    "walk-pr": "manifest-dev-tools",
    "teach-me": "manifest-dev-tools",
    "criteria-checker": "manifest-dev",
    "github-pr-lifecycle": "manifest-dev",
    "slack-poller": "manifest-dev",
    "prompt-reviewer": "manifest-dev-tools"
  },
  "runtime_owned_skills": {
    "do": "Harness-level Do is owned by the Pi runtime extension command, not distributed as a normal skill.",
    "done": "Done is a Pi runtime completion outcome, not a normal skill.",
    "escalate": "Escalation is a Pi runtime blocker outcome, not a normal skill."
  },
  "extension_wrappers": {
    "auto": "Provided by /auto (core package), which runs the manifest-dev lifecycle through the Pi Harness-level Do outcome gate.",
    "babysit-pr": "Provided by /babysit-pr (tools package), which synthesizes PR lifecycle grounding and runs it through the Pi Harness-level Do outcome gate."
  },
  "agents": {},
  "agents_excluded": {
    "_note": "Quality reviewers ship as the `code-review` skill (one dimension per invocation), not as agents. The remaining functional agents below are not shipped as dist/pi assets; verifier sessions are steered at runtime by the source-owned extension via each manifest gate's verify.prompt, and any agent prompt assets remain extension-private and source-owned.",
    "manifest-dev": [
      "criteria-checker",
      "github-pr-lifecycle",
      "slack-poller"
    ],
    "manifest-dev-tools": [
      "prompt-reviewer"
    ]
  },
  "commands": {
    "do": "Pi Harness-level Do entrypoint for an existing manifest (core package).",
    "auto": "Pi wrapper for autonomous figure-out -> define -> Harness-level Do (core package).",
    "babysit-pr": "Pi wrapper for PR lifecycle manifest synthesis and Harness-level Do (tools package)."
  },
  "tools": {},
  "runtime_internal": {
    "verification": "Runtime-owned verifier fanout over manifest Acceptance Criteria and Global Invariants; not exposed as an executor-callable tool.",
    "outcome": "Runtime-owned done and blocker outcomes after verifier fanout; not exposed as an executor-callable tool."
  },
  "runtime_dependencies": {
    "@gotgenes/pi-subagents": "Required for clean verifier subagent sessions."
  }
}
