---
name: define
description: 'Manifest builder. Turns shared understanding into a verifiable Manifest with Deliverables, Acceptance Criteria, Global Invariants, and Approach. Auto-invokes /figure-out when the transcript lacks understanding. Use when planning features, scoping refactors, debugging complex issues, or whenever a task needs structured thinking before coding. Triggers: define, manifest, scope, plan, spec out, break down.'
user-invocable: true
---

Take the conversation's shared understanding and encode it as a Manifest at `/tmp/manifest-{ts}.md`, plus an append-only discovery log at `/tmp/define-discovery-{ts}.md` (same `{ts}`, narrative compressed context — every decision that affects the manifest goes in). If the transcript lacks shared understanding, invoke `manifest-dev-experimental:figure-out` first ("no prior understanding detected — probing first"). Pure `--amend` invocations skip this front-door. The manifest captures **what to build** (Deliverables + ACs), **how to get there** (Approach — initial direction, expect adjustment), **rules to follow** (Global Invariants + Process Guidance + Known Assumptions + Risks + Trade-offs). Every insight becomes an encoded criterion (INV-G/AC/PG/R/T/ASM) or is explicitly scoped out with logged reasoning. Automate verification wherever possible; manual is last resort.

After writing, invoke the verifier: `Invoke the manifest-dev-experimental:manifest-verifier agent with: "Manifest: /tmp/manifest-{ts}.md | Log: /tmp/define-discovery-{ts}.md"` — verbatim labels, no framing, no commentary. **CONTINUE** → re-probe via figure-out on the verifier's questions, update manifest, re-verify. **COMPLETE** → emit a plain-language Summary for Approval (no codes, no YAML, no schema vocabulary; reads like talking to a colleague). On approval, output `Manifest complete:` per `references/COMPLETE.md`. If the user signals "enough" / "ship it" / "good enough" mid-loop, auto-decide remaining threads with recommended defaults (encoded with `(auto)` annotation and logged in Known Assumptions). Explicit `/do` invocation mid-flow → finish autonomously, hand off without summary approval.

**Pre-flight** resolves the run shape: babysit (`--babysit <pr-url>` → `references/BABYSIT_MODE.md`), amend (`--amend <path>` or `/tmp/manifest-*.md` in args, or Session-Default Detection from a prior `Manifest complete:` line → `references/AMENDMENT_MODE.md`), or fresh. Fresh branches with commits ahead of base fold those into Intent + starting Deliverables via Branch-Diff Seeding (base inference: upstream tracking → `origin/main` → `origin/master` → ask user). **Domain Guidance** loads task-type files from `tasks/` (CODING, FEATURE, BUG, REFACTOR, PR_LIFECYCLE, PROMPTING, WRITING, DOCUMENT, BLOG, RESEARCH) per `tasks/README.md` composition rules — quality gates auto-include as INV-G; Defaults as PG; risks/scenarios/trade-offs become probing fuel handed to figure-out. **Manifest schema** in `references/SCHEMA.md`. **ID scheme** in `references/IDS.md`. **Input flags** (`--amend`, `--from-do`, `--babysit`, `--platform`, `--canvas`) in `references/FLAGS.md`. **Multi-repo** in `references/MULTI_REPO.md`. **Canvas** in `references/CANVAS_MODE.md`.

**Coverage** (audited by manifest-verifier post-synthesis, not interview-driven): Domain Understanding (project-specific failure scenarios encoded), Reference Class (task type named + common failures covered), Failure Modes (scenarios have dispositions), Positive Dependencies (load-bearing assumptions surfaced), Process Self-Audit (scope-creep / edge-case-deferred / "temporary-becomes-permanent" patterns addressed; skippable for narrow single-deliverable scopes with no incremental-addition risk).
